name: contact
description: "Mobilní multiplayer appka — dva prsty, jeden prsten."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Supabase
  supabase_flutter: ^2.9.0

  # Navigation
  go_router: ^14.8.1

  # State management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7

  # Utils
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
